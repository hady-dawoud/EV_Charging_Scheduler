# Digital Twin Scripts

This folder holds runtime-facing scripts for the app-connected digital twin, including demo entrypoints, live request injection, runtime smoke checks, and app/runtime integration helpers.

Prefer these grouped paths for new commands. Legacy root-level wrappers are kept temporarily for backward compatibility.
