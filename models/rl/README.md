# RL Models

This folder is reserved for trained RL checkpoint artifacts and related model packaging outputs.

Current state:
- No checkpoint artifacts are committed.
- Deployment wiring is intentionally deferred to follow-up PRs.
