# ev_core.benchmarks

This package is the boundary for benchmark adapters and comparative evaluation harnesses.

Planned direction:
- Optional adapters for external benchmark suites (for example EV2Gym/SustainGym).
- No required dependency on benchmark packages for core runtime import paths.
- Keep adapter dependencies optional and explicitly configured.
